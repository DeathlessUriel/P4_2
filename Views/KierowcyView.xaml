<UserControl x:Class="TransportApp.Views.KierowcyView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

    <Grid Margin="20">

        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Text="Kierowcy"
                   FontSize="28"
                   FontWeight="Bold"
                   Margin="0 0 0 20"/>

        <Border Grid.Row="1"
                CornerRadius="10"
                BorderBrush="#DDD"
                BorderThickness="1">

            <DataGrid ItemsSource="{Binding Kierowcy}"
                      SelectedItem="{Binding SelectedKierowca}"
                      AutoGenerateColumns="False"
                      IsReadOnly="True">

                <DataGrid.Columns>

                    <DataGridTextColumn Header="Imię"
                                        Binding="{Binding Imie}"/>

                    <DataGridTextColumn Header="Nazwisko"
                                        Binding="{Binding Nazwisko}"/>

                    <DataGridTextColumn Header="Prawo jazdy"
                                        Binding="{Binding NumerPrawaJazdy}"/>

                    <DataGridTextColumn Header="Telefon"
                                        Binding="{Binding TelefonSluzbowy}"/>

                    <DataGridTextColumn Header="Karta"
                                        Binding="{Binding NumerKartyKierowcy}"/>

                    <DataGridTextColumn Header="Data zatrudnienia"
                                        Binding="{Binding DataZatrudnienia}"/>

                </DataGrid.Columns>

            </DataGrid>

        </Border>

        <StackPanel Grid.Row="2"
                    Orientation="Horizontal"
                    HorizontalAlignment="Right"
                    Margin="0 20 0 0">

            <Button Content="Dodaj"
                    Width="120"
                    Height="40"
                    Margin="5"
                    Command="{Binding AddCommand}"/>

            <Button Content="Edytuj"
                    Width="120"
                    Height="40"
                    Margin="5"
                    Command="{Binding EditCommand}"/>

            <Button Content="Usuń"
                    Width="120"
                    Height="40"
                    Margin="5"
                    Command="{Binding DeleteCommand}"/>

        </StackPanel>

    </Grid>
</UserControl>